import styled from 'styled-components';

const Content = styled.div`
    padding: 0 25px;
    height: 500px;
`

const PageNotFound = () => {
    return (
        <Content>
            PageNotFound
        </Content>
    );
}

export default PageNotFound;
